kafka:
  clusters:
    - name: local
      bootstrapServers: localhost:{{ kafka_port }}
      zookeeper: localhost:{{ zookeeper_client_port }}
      schemaRegistry: http://localhost:{{ schema_registry_port }}
      ksqldbServer: http://localhost:8088
      kafkaConnect:
        - name: first
          address: http://localhost:{{ kafka_connect_port }}
      metrics:
        port: 9997
        type: JMX
  #    -
  #      name: secondLocal
  #      bootstrapServers: localhost:9093
  #      zookeeper: localhost:2182
  #      schemaRegistry: http://localhost:18085
  #      kafkaConnect:
  #        - name: first
  #          address: http://localhost:8083
  #      metrics:
  #        port: 9998
  #        type: JMX
  #      read-only: true
  #    -
  #      name: localUsingProtobufFile
  #      bootstrapServers: localhost:9092
  #      protobufFile: messages.proto
  #      protobufMessageName: GenericMessage
  #      protobufMessageNameByTopic:
  #        input-topic: InputMessage
  #        output-topic: OutputMessage
spring:
  jmx:
    enabled: true
auth:
  type: DISABLED
server:
  port: {{ kafka_ui_port }} #- Port in which kafka-ui will run.